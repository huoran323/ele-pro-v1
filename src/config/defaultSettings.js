export default {
  // vue-ls options 本地存储方案
  storageOptions: {
    namespace: "vuejs_", // key prefix
    name: "ls", // name variable Vue.[ls] or this.[$ls],
    storage: "session" // storage name session, local, memory
  }
};
